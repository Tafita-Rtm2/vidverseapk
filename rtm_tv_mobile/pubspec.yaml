name: rtm_tv_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0

  # ✅ REMPLACE video_player + chewie
  # better_player supporte : HLS avec headers custom, retry auto, buffering robuste
  better_player: ^0.0.84

  cached_network_image: ^3.3.0
  carousel_slider: ^5.0.0
  flutter_blurhash: ^0.8.2
  google_fonts: ^6.1.0
  shimmer: ^3.0.0
  flutter_spinkit: ^5.2.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
